# 작업 리포트 — 2026-07-02

## 머지된 PR (1건)

| PR | 제목 | 스프린트 |
|----|------|---------|
| [#118](https://github.com/rumdice/platformA/pull/118) | GomokuRoom 핵심 게임 로직 단위 테스트 추가 (52→67개) | #84 |

> PR #118은 KST 기준 2026-07-02 머지됨 (UTC 표시 2026-07-01)

---

## 주요 작업 내용

### 1. GomokuRoom 단위 테스트 인프라 구축 및 15개 테스트 추가 (PR #118, 스프린트 #84)

- **배경**: 기존 52개 테스트(WinChecker, TurnManager, Board, GomokuRoomManager)는 순수 로직 계층만 커버. GomokuRoom 오케스트레이션(게임 중 연결 끊김 → 승패 결정, 5목 감지 → 게임 종료, 무승부 처리 등) 은 E2E에만 의존하는 커버리지 공백 상태.
- **테스트 인프라 신규 구축**:
  - `FakeGomokuSession` — GomokuSession 상속, TCP 소켓 없이 SessionId만 설정. `HandlePlaceStone(GomokuSession, ...)` 타입 체크 통과
  - `TestableGomokuRoom` — GomokuRoom 상속, `Broadcast()` override로 송신 패킷을 `BroadcastHistory List<byte[]>`에 캡처. TCP 전송 없이 패킷 내용 단언 가능
- **추가된 테스트 (15개)**:
  - `GomokuRoomDisconnectTests` (5개): WaitingPlayers 이탈 → SGameOver, 상태 유지 / InProgress 이탈 → Finished 전환, 상대방 WinnerId 확인 / Finished 후 재이탈 → 추가 브로드캐스트 없음(가드 작동)
  - `GomokuRoomPlaceStoneTests` (8개): 게임 전 돌 놓기 무시 / 순서 아닌 플레이어 거부 / 이미 점유된 칸 거부 / 유효한 수 → SBoardUpdate 브로드캐스트 / 턴 전환 / 5목 완성 → Finished / SGameOver WinnerId 검증 / 보드 가득 → Draw(WinnerId=0)
  - `GomokuRoomFinishGameGuardTests` (2개): Enter×2 → SGameStart 브로드캐스트 / 5목 후 HandleDisconnect → SGameOver 중복 없음
- **결과**: 기존 로직이 모두 올바름을 확인(버그 미발견). 전체 246개 솔루션 테스트 통과.
- **E2E 시나리오 10 실행**: OVERALL PASS 확인 — 기존 통합 경로 회귀 없음.
- **Redis 키 TTL 전수 조사**: 모든 키가 TTL을 가지거나 값만 업데이트되는 구조(stats:{code})임을 확인. 영구 누적 데이터 없음.

---

## 프로젝트 완성도 현황

| 프로젝트 | 완성도 | 빌드/테스트 | 핵심 기능 | 운영 준비 | 문서화 | 비고 |
|---------|--------|------------|---------|---------|------|------|
| Auth.API | 95% | ✅ (24개) | ✅ | ✅ | ✅ | JWT·리프레시·Rate Limit(username) 완비 |
| Ticketing.API | 90% | ✅ (21개) | ✅ | ✅ | ✅ | 큐·하트비트·Ghost Cleanup 완비 |
| Matching.API | 90% | ✅ (28개) | ✅ | ✅ | ✅ | ELO·매치레코드·Redis Pub/Sub 완비 |
| Game.Gomoku | 88% | ✅ (67개) | ✅ | ✅ | N/A | TCP 게임서버, 오케스트레이션 테스트 이번에 추가 |
| Game.Lobby | 83% | ✅ (74개) | ✅ | ✅ | N/A | SignalR 허브·매치 요청 라우팅 완비, 부하 테스트 미완 |
| Utils.API | 88% | ✅ (32개) | ✅ | ✅ | ✅ | URL 단축·통계 write-back 완비 |
| Library.Game | 90% | ✅ | ✅ | N/A | N/A | IGameSession/Room/Manager 인터페이스 + 추상 기반 클래스 완비 |

**전체 플랫폼 완성도**: **89%** — 핵심 기능과 운영 준비는 완비, Game.Lobby 부하 테스트 및 게임 추가 확장이 남은 주요 과제.

---

## 오늘 작업 피드백 및 개선점

### 1. 테스트 인프라 설계 접근

**잘된 점**: `TestableGomokuRoom(Broadcast override) + FakeGomokuSession(GomokuSession 상속)` 조합으로 TCP 소켓 없이 게임 오케스트레이션 레이어를 완전히 격리해 테스트. Moq나 추가 패키지 없이 C# 상속만으로 해결한 것이 깔끔함.

**개선하면 좋을 점**: CS1628(out 파라미터 람다 캡처 금지) 오류를 테스트 작성 후 발견해 수정이 필요했음. 작성 전 `room.Push()` 안에서 out 파라미터를 사용하지 못한다는 제약을 미리 인지했다면 첫 번째 빌드에 통과 가능했음.

### 2. 기존 구현 신뢰성 검증

**잘된 점**: 15개 테스트 전부 최초 실행에서 통과 — GomokuRoom의 기존 HandlePlaceStone, HandleDisconnect, FinishGame 가드 로직이 처음부터 올바르게 구현되어 있었음을 공식 확인.

**개선하면 좋을 점**: 신규 기능 추가 시 테스트를 코드와 동시에 작성하는 TDD 방식으로 전환하면 "나중에 검증 추가" 단계를 별도로 진행하지 않아도 됨.

### 3. 테스트 범위 선정

**잘된 점**: 무승부(보드 가득) 케이스를 Reflection으로 `_board` 필드에 224개 선행 채워 마지막 한 수만 HandlePlaceStone으로 두는 방식으로 처리 — E2E로는 재현 불가능한 경계 케이스를 단위 테스트에서 정확히 커버.

**개선하면 좋을 점**: 턴 타임아웃(StartGame 내 Task.Run 루프) 경로는 실제 타이머 대기가 필요해 이번 테스트에서 제외됨. `FakeTimer` 또는 시간 주입 패턴 도입 시 타임아웃 경로도 단위 테스트 가능.

### 4. E2E + 단위 테스트 역할 분담 명확화

**잘된 점**: E2E 시나리오 10(1000명 + Failover C)로 통합 경로 회귀를 확인하고, 단위 테스트로 거부/가드/무승부 등 E2E에서 자연 발생하지 않는 경계 케이스를 보완 — 두 레이어가 역할을 명확히 분담.

**개선하면 좋을 점**: 현재 E2E 시나리오는 정상 게임 완주와 Failover만 검증. 의도적으로 잘못된 순서의 돌 놓기나 동시 패킷 등 비정상 입력 시나리오를 E2E 레벨에서도 일부 추가하면 계층 간 커버리지 격차를 더 좁힐 수 있음.

---

## 종료 시점 상태

- 오픈 PR: 없음
- 스프린트 #84 상태: 완료
- E2E 시나리오 10: OVERALL PASS
- 특이사항: Redis 키 TTL 전수 조사 완료 — 영구 누적 데이터 없음 확인
