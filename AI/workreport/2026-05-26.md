# 작업 리포트 — 2026-05-26

## 머지된 PR (2건)

| PR | 제목 | 스프린트 |
|----|------|---------|
| [#55](https://github.com/rumdice/platformA/pull/55) | feat(sdlc): GitHub Actions Gate Check 및 주간 리포트 기반 마련 | #27 |
| [#56](https://github.com/rumdice/platformA/pull/56) | 비용 로그 메트릭 강화 및 자동 기록 스킬 개선 | #28 |

---

## 주요 작업 내용

### 1. GitHub Actions SDLC Gate Check 및 주간 리포트 (PR #55, 스프린트 #27) — M

**배경**: AI_SDLC 파이프라인에서 PR 병합 전 단계 검사가 수동이었다. 리뷰·임팩트 분석·테스트 생성 완료 여부를 자동으로 확인하는 게이트가 필요했고, 누적 데이터를 활용한 주간 리포트 기반도 마련해야 했다.

- `.github/workflows/sdlc-gate-check.yml` — PR 오픈 시 task JSON의 `review_completed`, `impact`, `test_generated` 필드를 검사하는 CI 게이트 워크플로우 추가
- `.github/scripts/check_sdlc_gate.py` — gate check 실제 검사 로직 구현 (test/review/impact 세 조건)
- `.github/scripts/generate_sdlc_report.py` — tasks/ 디렉터리를 순회하여 스프린트별 완료율·비용·리스크를 집계하는 주간 리포트 스크립트 신규
- `.github/scripts/sync_merged_pr.py` — steps[] 기록, cost-log 중복 방지, summary 출력 기능 추가
- `AI/tasks/SCHEMA.md` — GitHub Actions 연동 필드 설명 보강
- `AI/AI_SDLC(pipeline).txt` — 단계 10(gate check), 11(sync) 반영

빌드 0 오류, 테스트 113/113 통과. 변경 파일 8개 전부 Python/YAML/문서 — 런타임 리스크 LOW.

---

### 2. cost-log 메트릭 강화 및 자동 계산 (PR #56, 스프린트 #28) — M

**배경**: 기존 cost-log에는 작업 규모(S/M/L/XL)만 기록되고 실제 소요 시간과 토큰 소비량이 없었다. 작업 비용을 정량적으로 추적하고 향후 예산 계획에 활용하기 위해 세 컬럼을 추가했다.

- `AI/cost-log.md` — `duration_sec`, `consume_tokens`, `cache_tokens` 컬럼 추가
- `AI/tasks/SCHEMA.md` — 세 필드 정의 및 자동 계산 가이드 문서화
- `.claude/skills/plan/SKILL.md` — task JSON 템플릿에 세 필드 초기값 추가
- `.claude/skills/pr/SKILL.md` — `/done` 시 `count_tokens.py`를 호출하여 JSONL 기반 자동 계산 및 기록
- `.github/scripts/sync_merged_pr.py` — `append_cost_log()` 함수에 `cache_tokens` 컬럼 포함
- `.github/scripts/count_tokens.py` — Claude Code JSONL 로그를 파싱하여 `consume_tokens`·`cache_tokens`·`duration_sec`을 계산하는 스크립트 신규

소요 시간: 2208초 / 소비 토큰: 16,096,374 / 캐시 토큰: 654,245. 테스트 113개 전부 통과.

---

## 종료 시점 상태

- 오픈 PR: 없음
- 스프린트 #27 상태: 완료
- 스프린트 #28 상태: 완료
- 특이사항: 빌드 캐시 오류(MSB3492) 세션 시작 시 감지됨 — `dotnet clean && dotnet build`로 해소 필요
