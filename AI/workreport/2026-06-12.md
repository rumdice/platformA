# 작업 리포트 — 2026-06-12

## 머지된 PR (2건)

| PR | 제목 | 스프린트 |
|----|------|---------|
| [#94](https://github.com/rumdice/platformA/pull/94) | fix: Phase C 이후 SPRINT.md/cost-log.md 참조 정리 | #62 |
| [#95](https://github.com/rumdice/platformA/pull/95) | fix: GitHub Actions DB 격리 위반 제거 및 n8n CI 실패 감지 커서 기반 전환 | #63 |

---

## 주요 작업 내용

### 1. Phase C 참조 정리 (PR #94, Sprint #62)

이전 Sprint에서 `AI/SPRINT.md`와 `AI/cost-log.md`가 삭제되었으나 이를 참조하는 코드가 4개 파일에 잔존하여 `/sprint` 커맨드 빈 결과, `/workreport` sidecar 오류, `pr-merge-sync.yml` CI 경고, `Docs/operations/` 고아 파일 문제가 발생했다.

- `.claude/commands/sprint.md` — `AI/SPRINT.md` 직접 읽기를 제거하고 DB + `AI/sprints/` 폴백 방식으로 교체
- `.claude/skills/workreport/SKILL.md` — `tail AI/SPRINT.md`·`grep cost-log.md` sidecar 제거, DB/sprints 기반 데이터 수집으로 전환
- `.github/workflows/pr-merge-sync.yml` — 삭제된 파일(`AI/SPRINT.md`, `AI/cost-log.md`) `git add` 제거
- `Docs/operations/ai-sdlc-*.md` 6개 삭제 — `Docs/ai-sdlc/`로 이미 이동된 고아 파일
- `session-start.sh` — 죽은 변수 `SPRINT_FILE` 제거

**배경**: AI SDLC Phase C(DB 단독 진실원) 전환 후 파일 기반 참조 코드를 정리하는 사후 처리 작업. 기능 추가 없이 오작동 제거.

---

### 2. GitHub Actions DB 격리 위반 제거 및 n8n CI 실패 감지 강화 (PR #95, Sprint #63, XL 규모)

Sprint #62 완료 후 AI SDLC 완성도 88% 평가 결과를 기반으로 아키텍처 격리 원칙 위반과 n8n 감지 신뢰성 문제를 동시에 해결했다.

**DB 격리 원칙 위반 제거:**
- `.github/workflows/auto-fix.yml` 삭제 — `ANTHROPIC_API_KEY` 미등록으로 실제로는 실행 불가 상태였으나, 별도 Anthropic API 과금을 요구하는 구조 자체를 제거
- `.github/scripts/generate_sprint_md.py` 삭제 — 갱신 대상인 `AI/SPRINT.md`가 Phase C에서 이미 삭제됨(이중 불필요), `psycopg2`로 로컬 DB 접근 시도 포함
- `.github/workflows/pr-merge-sync.yml` — `generate_sprint_md.py`를 호출하던 "Sync sprint files (DB-based)" step 제거. `SDLC_DB_CONNECTION` secret 참조 완전 소멸

**n8n CI 실패 감지 커서 기반 전환:**
- `github-failure-monitor.json` 11노드 → 7노드로 축소. `filter-recent`(15분 필터), `fixable-filter`, `postgres-job-lock-claim`, `check-lock-claimed`, `github-dispatch` 5개 제거
- `커서 읽기·갱신` 노드 추가 — n8n Static Data에 `last_checked_at` 저장. 초기값 24시간 전(노트북 재시작 후 소급 처리 보장). 낙관적 갱신 + `ON CONFLICT DO NOTHING` 중복 방어
- `github-get-failed-runs` 수정 — `created >= last_checked_at` 파라미터 추가, `per_page` 5→20
- 부가 수정: `classify-failure` 노드의 `$('split-runs')` (node ID 오참조 버그) → `$('각 실패 run 분리')` (node name 정상 참조)

**CI 실패 알림 추가:**
- `.claude/skills/workflow/SKILL.md` — 0.5단계 삽입: 작업 시작 전 `record_failure.py --list-unresolved` 실행하여 미해결 CI 실패 표시
- `.claude/hooks/session-start.sh` — 4.5단계 추가: main 브랜치 세션 시작 시 전체 미해결 CI 실패 목록 출력

**처리 규모**: 소비 토큰 11.8M, 캐시 토큰 312K, 소요 시간 약 13분 (XL)

---

## 종료 시점 상태

- 오픈 PR: 없음
- 스프린트 #62, #63 상태: 완료
- n8n `GitHub CI Failure Monitor`: Published (7노드, 커서 기반 활성)
- AI SDLC 완성도: **약 93%** (Sprint #63 완료 후)
- 잔여 과제: `SDLC_DB_CONNECTION` GitHub Secret 수동 삭제 권장 (참조 워크플로우 0개)
