# 작업 리포트 — 2026-06-04

## 머지된 PR (2건)

| PR | 제목 | 스프린트 |
|----|------|---------|
| #70 | feat: PlatformA.SdlcDB.Lib — AI_SDLC Phase 3 PostgreSQL EF Core 상태 저장소 MVP | #41 |
| #71 | feat: CI 실패 자동 감지·기록·수정 파이프라인 (n8n + PostgreSQL 기반) | #42 |

---

## 주요 작업 내용

### 1. PlatformA.SdlcDB.Lib — PostgreSQL EF Core 상태 저장소 (PR #70, 스프린트 #41)

- `PlatformA.SdlcDB.Lib` 신규 프로젝트 생성 및 솔루션 등록 (Npgsql 9.x)
- `AiJob` / `AiJobStep` / `AiFailure` / `AiModelRun` 4개 Entity 설계 및 구현
- `SdlcDbContext` + `IDesignTimeDbContextFactory` 구성 — `sdlc` 스키마 분리로 n8n 스키마 충돌 방지
- `InitialSdlcDb` EF Core Migration 생성 및 `dotnet ef database update` 검증
- `Dockerfile.migrator` 작성 (CMD 패턴, dotnet-ef 9.*)
- `docker-compose.full.yml` + `docker/postgresql/docker-compose.yml`에 `sdlc-db-migrator` 서비스 추가
- `.github/scripts/migrate_tasks_to_postgres.py --dry-run` 구현 — 기존 task JSON → PostgreSQL 이관 스크립트
- 영향 범위: HIGH risk 분류 (Migrations/Entities/Context 신규), 실제로는 신규 sdlc 스키마에만 영향, 기존 게임 서비스 무영향

### 2. CI 실패 자동 감지·기록·수정 파이프라인 (PR #71, 스프린트 #42, 규모: M)

- `/done` 스킬에 `dotnet format --verify-no-changes` 검증 추가 — format 실패 시 자동 수정·재커밋
- `.github/workflows/auto-format.yml` 신규 — CI format 실패 감지 시 fix 커밋 후 워크플로 재실행
- `.github/scripts/record_failure.py` — `ai_failures` 테이블 PostgreSQL INSERT 헬퍼
- `.github/scripts/check_sdlc_gate.py` 수정 — gate 실패 시 `record_failure` 자동 기록
- `.n8n/workflows/github-failure-monitor.json` — GitHub API 폴링 → 실패 감지 → PostgreSQL INSERT 워크플로
- `.claude/hooks/session-start.sh` 수정 — 세션 시작 시 `ai_failures` 미해결 건 자동 조회·표시
- ADR-008(n8n) · ADR-009(PostgreSQL) 범위 내 구현, 신규 기술 도입 없음

---

## 종료 시점 상태

- 오픈 PR: 없음
- 스프린트 #41 상태: 완료 (PR #70 머지)
- 스프린트 #42 상태: 완료 (PR #71 머지)
- 특이사항: AI_SDLC Phase 3 PostgreSQL 상태 저장소 MVP 구축 완료. CI 실패 자동 감지·기록·수정 5레이어 파이프라인 구성 완료.
